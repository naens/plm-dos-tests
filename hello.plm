/****p* plm-exercises/hello
 *  NAME
 *    hello -- Print 'HELLO!' and exit
 *  DESCRIPTION
 *    Print 'HELLO!' and exit.
 *  USES
 *    cons and sys modules
 ****
 */
hello:
do;

    prstr: procedure (pstr) external;
        declare p$str address;
    end prstr;

    term: procedure external;
    end term;

    call prstr(.('HELLO!', 13, 10, 0));
    call term;

end hello;
